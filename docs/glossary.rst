.. include:: links.rst

.. glossary::

   :math:`\Delta{M}`
      Delta-M is the difference between control and label volumes in the ASL time series.
      It is typically retained as a time series, in which each volume is the result of
      subtracting two neighboring volumes.

      .. math::
         \Delta{M} = M_{C} - M_{L}
