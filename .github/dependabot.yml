# To get started with Dependabot version updates, you'll need to specify which
# package ecosystems to update and where the package manifests are located.
# Please see the documentation for all configuration options:
# https://docs.github.com/github/administering-a-repository/configuration-options-for-dependency-updates

version: 2
updates:
  - package-ecosystem: "github-actions" # See documentation for possible values
    directory: "/" # Location of package manifests
    labels: ["maintenance", "ignore-for-release"]
    assignees: ["tsalo"]
    schedule:
      interval: "weekly"
  # Pip: [project].dependencies and optional-dependencies in pyproject.toml (PyPI).
  # [tool.pixi.dependencies] (Conda/FSL/ANTs) are not updated by Dependabot; update those manually and run pixi lock.
  - package-ecosystem: "pip"
    directory: "/"
    labels: ["maintenance", "ignore-for-release"]
    assignees: ["tsalo"]
    schedule:
      interval: "monthly"
